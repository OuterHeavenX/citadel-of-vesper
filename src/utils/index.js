/**
 * src/utils — pure helpers, no Phaser, no game state.
 *
 * Public API surface:
 *   math.js    clamp, lerp, rand, randInt, choose, sign
 *   format.js  formatTime(seconds) -> 'H:MM:SS', formatNumber
 *   pool.js    class Pool — generic object pool (used by ProjectileSystem, effects)
 */
export const UtilsInfo = 'see math.js, format.js, pool.js (Wave 1)';
