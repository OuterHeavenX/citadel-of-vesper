/**
 * src/bosses — ORIGINAL boss designs only. Public API surface:
 *
 *   Boss.js  class Boss — entrance -> phases -> death; getHp/getMaxHp/getPhase
 *
 * Boss definitions: data/bosses.json (schema: data/schemas/boss.schema.json).
 * Vertical slice: 1 miniboss + 1 major boss (Wave 4).
 */
export { Boss } from './Boss.js';
